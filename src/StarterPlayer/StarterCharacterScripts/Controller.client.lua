-- Handles all client-related code dependent of its character

local ReplicatedStorage: ReplicatedStorage = game:GetService("ReplicatedStorage")
local rocket = require(ReplicatedStorage.Modules.Rocket)

rocket()